# apps/shared

App-specific hooks shared across an app family. Stub for now.

Deferred: a per-app `describe(x,y)` hook supplying quadrant -> text labels
for self-labeling dropped nodes in edit mode (see spec.md "Deferred").
